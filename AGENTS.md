<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan.

Use GitHub Spec Kit for feature work in this repository. Prefer the Spec Kit
workflow before implementation:

1. Establish or update principles with `speckit-constitution`.
2. Define requirements with `speckit-specify`.
3. Create the technical plan with `speckit-plan`.
4. Generate tasks with `speckit-tasks`.
5. Implement from those tasks with `speckit-implement`.

Codex skills are installed at the Codespace level under
`/home/codespace/.codex/skills/speckit-*` because this Codespace mounts
`.agents` read-only. Claude skills are installed in `.claude/skills/speckit-*`.
<!-- SPECKIT END -->
